# Структура маршрутизации в Expo Router

## Обзор

Реализовать чистую, организованную систему маршрутизации с использованием file-based маршрутизации Expo Router и группы маршрутов (route groups). Структура отделяет потоки аутентифицированных и неаутентифицированных пользователей, сохраняя при этом плоскую структуру URL.

---

## 1. Корневой макет

Корневой макет служит основным контейнером навигации, который управляет всеми группами маршрутов:

```typescript
function RootNavigation() {
  return (
    <Stack>
      <Stack.Screen name="index" options={{ headerShown: false }} />
      <Stack.Screen name="(main)" options={{ headerShown: false }} />
      <Stack.Screen name="(auth)" options={{ headerShown: false }} />
    </Stack>
  );
}
```

### Структура корневого макета:

| Экран      | Назначение                                 | Заголовок |
| ---------- | ------------------------------------------ | --------- |
| **index**  | Входная точка / логика перенаправления     | Скрыт     |
| **(main)** | Экраны аутентифицированных пользователей   | Скрыт     |
| **(auth)** | Экраны аутентификации (логин, регистрация) | Скрыт     |

### Ключевые характеристики:

- **Без заголовков**: Все экраны используют `headerShown: false` для пользовательской навигации
- **Stack навигация**: Линейный поток между группами маршрутов
- **Модульный дизайн**: Каждая группа управляет своей собственной навигацией

---

## 2. Входная точка

Корневой экран index обрабатывает логику начального перенаправления:

```typescript
// app/index.tsx
export default function Index() {
  return <Redirect href={RoutePaths.HomeScreen} />;
}
```

### Поведение:

1. **Пользователь открывает приложение** → Рендерится экран Index
2. **Немедленное перенаправление** → Перенаправляет на `RoutePaths.HomeScreen`
3. **AuthProvider определяет пункт назначения**:
   - Пользователь аутентифицирован → Перенаправление в основное приложение
   - Пользователь не аутентифицирован → Перенаправление на приветственный/логин экран

---

## 3. Принципы работы

### 3.1 File-Based маршрутизация

Expo Router автоматически создаёт маршруты на основе структуры файлов:

```
app/
├── index.tsx                    → Маршрут: "/"
├── (auth)/                      → Группа маршрутов (без URL сегмента)
│   ├── _layout.tsx             → Общий макет для экранов аутентификации
│   ├── login.tsx               → Маршрут: "/login"
│   ├── register.tsx            → Маршрут: "/register"
│   └── forgot-password.tsx      → Маршрут: "/forgot-password"
├── (main)/                      → Группа маршрутов (без URL сегмента)
│   ├── _layout.tsx             → Макет основного приложения
│   ├── (tabs)/                 → Вложенная группа маршрутов
│   │   ├── _layout.tsx         → Настройка навигации вкладок
│   │   └── index.tsx           → Маршрут: "/"
│   ├── send-message.tsx        → Маршрут: "/send-message"
│   ├── lock.tsx                → Маршрут: "/lock"
│   └── document-viewer/
│       └── [id].tsx            → Маршрут: "/document-viewer/:id"
└── _layout.tsx                 → Корневая обёртка макета
```

### 3.2 Stack навигация

Все экраны используют Stack навигацию без видимых заголовков:

```typescript
// app/_layout.tsx (корневой)
export default function RootLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        animationEnabled: true
      }}
    >
      <Stack.Screen name="index" />
      <Stack.Screen name="(auth)" />
      <Stack.Screen name="(main)" />
    </Stack>
  );
}
```

**Преимущества:**

- Чистый, минималистичный UI
- Пользовательские компоненты навигации (заголовки, кнопки)
- Полный контроль над внешним видом

### 3.3 Логика перенаправления

Условная навигация на основе состояния аутентификации:

```typescript
// app/index.tsx - входная точка
export default function Index() {
  const { isAuthenticated } = useAuthStore();

  // Перенаправить на основе состояния аутентификации
  if (isAuthenticated) {
    return <Redirect href={RoutePaths.HomeScreen} />;
  } else {
    return <Redirect href={RoutePaths.WelcomeScreen} />;
  }
}

// app/(main)/_layout.tsx - защита основного приложения
export default function MainLayout() {
  const { isAuthenticatedUser } = useAuthStore();

  if (!isAuthenticatedUser) {
    return <Redirect href={RoutePaths.WelcomeScreen} />;
  }

  return (
    <Tabs
      screenOptions={{
        headerShown: true
      }}
    >
      <Tabs.Screen name="(tabs)" />
      <Tabs.Screen name="send-message" />
      <Tabs.Screen name="lock" />
    </Tabs>
  );
}
```

---

## 4. Группы маршрутов (скобки)

### Что такое группы маршрутов?

Группы маршрутов создаются с использованием скобок в названиях папок. Они:

- **Организуют код логически** без влияния на структуру URL
- **Создают изолированные стеки навигации** на группу
- **Не добавляют URL сегменты** (чистые маршруты)

### Примеры групп маршрутов:

#### Пример 1: Группа аутентификации

```
app/(auth)/
├── _layout.tsx
├── login.tsx            → URL: "/login"
├── register.tsx         → URL: "/register"
└── forgot-password.tsx  → URL: "/forgot-password"
```

- **Имя папки со скобками**: `(auth)` не отображается в URL
- **Чистые URL**: `/login`, `/register` (не `/auth/login`)
- **Общий макет**: Все экраны аутентификации используют один макет/анимации

#### Пример 2: Группа основного приложения

```
app/(main)/
├── _layout.tsx
├── (tabs)/
│   ├── _layout.tsx
│   ├── messages.tsx     → URL: "/messages"
│   ├── profile.tsx      → URL: "/profile"
│   └── settings.tsx     → URL: "/settings"
├── send-message.tsx     → URL: "/send-message"
└── document-viewer/
    └── [id].tsx         → URL: "/document-viewer/:id"
```

- **Сгруппировано но чисто**: `/messages`, `/profile` (не `/main/messages`)
- **Вложенные группы**: `(tabs)` внутри `(main)`
- **Навигация вкладок**: Общая панель вкладок для сгруппированных экранов

---

## 5. Сравнение URL: с скобками и без

### ❌ БЕЗ групп маршрутов (добавляет URL сегменты)

```
app/main/tabs/messages.tsx      → URL: /main/tabs/messages
app/main/send-message.tsx       → URL: /main/send-message
app/auth/login.tsx              → URL: /auth/login
app/auth/register.tsx           → URL: /auth/register
```

**Проблемы:**

- URL раскрывают внутреннюю структуру
- Длинные, сложные для запоминания
- Сложно рефакторить без нарушения URL

### ✅ СО группами маршрутов (чистые URL)

```
app/(main)/(tabs)/messages.tsx  → URL: /messages
app/(main)/send-message.tsx     → URL: /send-message
app/(auth)/login.tsx            → URL: /login
app/(auth)/register.tsx         → URL: /register
```

**Преимущества:**

- Чистые, семантические URL
- Внутренняя структура скрыта
- Лёгко переорганизовать без изменения URL
- Лучше для deep linking и закладок

---

## 6. Полная структура приложения

### Организация файлов

```
app/
├── _layout.tsx                      # Корневая обёртка макета
├── index.tsx                        # Входная точка / логика перенаправления
│
├── (auth)/                          # 🔓 Группа маршрутов аутентификации
│   ├── _layout.tsx                 # Макет экрана аутентификации
│   ├── login.tsx                   # /login
│   ├── register.tsx                # /register
│   ├── forgot-password.tsx         # /forgot-password
│   └── verify-email.tsx            # /verify-email
│
└── (main)/                          # 🔐 Группа маршрутов аутентифицированных пользователей
    ├── _layout.tsx                 # Макет основного приложения с защитой
    ├── (tabs)/                     # Группа вкладок (без URL сегмента)
    │   ├── _layout.tsx            # Навигатор вкладок
    │   ├── messages.tsx            # /messages
    │   ├── profile.tsx             # /profile
    │   └── settings.tsx            # /settings
    ├── send-message.tsx            # /send-message
    ├── message-detail/
    │   └── [id].tsx                # /message-detail/:id
    ├── document-viewer/
    │   └── [id].tsx                # /document-viewer/:id
    └── lock.tsx                    # /lock
```

### Карта маршрутизации URL

| Путь файла                        | URL                    | Назначение                            |
| --------------------------------- | ---------------------- | ------------------------------------- |
| `index.tsx`                       | `/`                    | Входная точка, логика перенаправления |
| `(auth)/login.tsx`                | `/login`               | Экран логина                          |
| `(auth)/register.tsx`             | `/register`            | Экран регистрации                     |
| `(main)/(tabs)/messages.tsx`      | `/messages`            | Список сообщений                      |
| `(main)/(tabs)/profile.tsx`       | `/profile`             | Профиль пользователя                  |
| `(main)/message-detail/[id].tsx`  | `/message-detail/123`  | Детали сообщения                      |
| `(main)/document-viewer/[id].tsx` | `/document-viewer/abc` | Просмотр файла                        |

---

## 7. Реализация защиты аутентификации

### Защита на корневом уровне

```typescript
// app/(main)/_layout.tsx
import { useAuthStore } from "@/store/authStore";
import { useRouter } from "expo-router";

export default function MainLayout() {
  const { accessToken, refreshToken } = useAuthStore();
  const router = useRouter();

  // Проверка аутентификации пользователя
  const isAuthenticatedUser = !!accessToken && !!refreshToken;

  useEffect(() => {
    if (!isAuthenticatedUser) {
      // Перенаправить на приветственный экран если не аутентифицирован
      router.replace(RoutePaths.WelcomeScreen);
    }
  }, [isAuthenticatedUser]);

  if (!isAuthenticatedUser) {
    return <LoadingSpinner />;
  }

  return (
    <Tabs
      screenOptions={{
        headerShown: true,
        tabBarLabelPosition: "below-icon"
      }}
    >
      <Tabs.Screen
        name="(tabs)"
        options={{
          title: "Сообщения",
          tabBarIcon: ({ color }) => <MessageIcon color={color} />
        }}
      />
      <Tabs.Screen
        name="profile"
        options={{
          title: "Профиль",
          tabBarIcon: ({ color }) => <ProfileIcon color={color} />
        }}
      />
      <Tabs.Screen
        name="settings"
        options={{
          title: "Параметры",
          tabBarIcon: ({ color }) => <SettingsIcon color={color} />
        }}
      />
    </Tabs>
  );
}
```

### Макет группы аутентификации

```typescript
// app/(auth)/_layout.tsx
import { Stack } from "expo-router";

export default function AuthLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        gestureEnabled: false,
        animationEnabled: true
      }}
    >
      <Stack.Screen
        name="login"
        options={{
          animationTypeForReplace: "pop"
        }}
      />
      <Stack.Screen
        name="register"
        options={{
          animationTypeForReplace: "fade"
        }}
      />
      <Stack.Screen name="forgot-password" />
    </Stack>
  );
}
```

---

## 8. Примеры навигации

### Программная навигация

```typescript
import { useRouter } from "expo-router";

function LoginButton() {
  const router = useRouter();

  const handleLogin = async () => {
    // Выполнить логин
    await login(email, password);

    // Навигация в основное приложение
    router.replace("/messages"); // Чистый URL
    // или
    router.replace("/(main)/(tabs)/messages"); // Имя маршрута
  };

  return <Button onPress={handleLogin} title="Войти" />;
}
```

### Навигация к динамическому маршруту

```typescript
function MessageListScreen() {
  const router = useRouter();

  const handleSelectMessage = (messageId: string) => {
    // Навигация с параметром
    router.push(`/message-detail/${messageId}`);
    // Чистый URL: /message-detail/123
  };

  return (
    <FlatList
      data={messages}
      renderItem={({ item }) => (
        <MessageItem
          message={item}
          onPress={() => handleSelectMessage(item.id)}
        />
      )}
    />
  );
}
```

### Обработка Deep Links

```typescript
// Автоматически работает с чистыми URL
// Deep link: app://messages → /messages
// Deep link: app://message-detail/123 → /message-detail/123
// Deep link: app://login → /login
```

---

## 9. Справочник имён маршрутов

Определите централизованные константы маршрутов, чтобы избежать магических строк:

```typescript
// constants/RoutePaths.ts
export const RoutePaths = {
  // Входная точка
  Index: "/",

  // Маршруты аутентификации
  WelcomeScreen: "/welcome",
  LoginScreen: "/login",
  RegisterScreen: "/register",
  ForgotPasswordScreen: "/forgot-password",
  VerifyEmailScreen: "/verify-email",

  // Маршруты основного приложения
  HomeScreen: "/messages",
  ProfileScreen: "/profile",
  SettingsScreen: "/settings",

  // Динамические маршруты
  MessageDetailScreen: (id: string) => `/message-detail/${id}`,
  DocumentViewerScreen: (id: string) => `/document-viewer/${id}`,
  SendMessageScreen: "/send-message",
  LockScreen: "/lock"
};

// Использование
router.push(RoutePaths.MessageDetailScreen("msg-123"));
router.replace(RoutePaths.HomeScreen);
<Link href={RoutePaths.LoginScreen} />
```

---

## 10. Преимущества групп маршрутов

### Преимущества организации

```
✅ Логическая группировка без загрязнения URL
✅ Отдельные макеты на группу (auth vs. main)
✅ Изолированные стеки навигации
✅ Простая рефакторизация без изменения URL
✅ Чистые, семантические URL
```

### Опыт разработчика

```
✅ Самодокументируемая структура
✅ Чёткие границы аутентификации
✅ Типобезопасная навигация по маршрутам (с надлежащей типизацией)
✅ Проще сотрудничеству
✅ Лучше для адаптации новых разработчиков
```

### Опыт пользователя

```
✅ Чистые, легко запоминаемые URL
✅ Поддерживаемые deep links
✅ Профессиональная маршрутизация
✅ Консистентные паттерны навигации
✅ Более быстрое восприятие производительности
```

---

## 11. Сравнение: группа маршрутов против обычной папки

### Группа маршрутов (рекомендуется)

```typescript
// app/(main)/messages.tsx
// URL: /messages
// Чёткое разделение, никакого загрязнения URL
// Может иметь общий (main) макет
```

### Обычная папка

```typescript
// app/main/messages.tsx
// URL: /main/messages
// URL раскрывает структуру
// Сложнее рефакторить
```

---

## 12. Тестирование маршрутов

```typescript
// __tests__/routing.test.tsx
import { render, screen } from "@testing-library/react-native";
import Index from "@/app/index";

describe("Маршрутизация", () => {
  test("должно перенаправить аутентифицированных пользователей на главный экран", () => {
    // Моккировать хранилище аутентификации
    useAuthStore.setState({
      accessToken: "token123",
      refreshToken: "refresh123"
    });

    render(<Index />);
    // Должно перенаправить на /messages
  });

  test("должно перенаправить неаутентифицированных пользователей на логин", () => {
    // Моккировать хранилище аутентификации (без токенов)
    useAuthStore.setState({
      accessToken: null,
      refreshToken: null
    });

    render(<Index />);
    // Должно перенаправить на /login
  });

  test("должно предотвратить доступ к основным маршрутам без аутентификации", () => {
    // Пользователь пытается навигировать на /messages без аутентификации
    // Должно перенаправить на /login
  });
});
```

---

## 13. Итоговая сводка: ключевые моменты

- **File-based маршрутизация**
  - Реализация: Имена файлов автоматически отображаются на маршруты
  - Преимущество: Предсказуемо, масштабируемо

- **Группы маршрутов**
  - Реализация: Использовать скобки `(name)`
  - Преимущество: Чистые URL, логическая организация

- **Stack навигация**
  - Реализация: Все экраны в контейнере Stack
  - Преимущество: Плавные анимации, навигация назад

- **Защита аутентификации**
  - Реализация: Проверка токенов в макете (main)
  - Преимущество: Предотвращение несанкционированного доступа

- **Логика перенаправления**
  - Реализация: Маршрут на основе состояния аутентификации
  - Преимущество: Прозрачный опыт пользователя

- **Динамические маршруты**
  - Реализация: Использование синтаксиса `[id].tsx`
  - Преимущество: Обработка параметризованной навигации

- **Deep linking**
  - Реализация: Маршруты автоматически работают как deep links
  - Преимущество: Поделиться и открыть URL

---

## Полный поток навигации

```
┌─────────────────────────────────────────┐
│    ПРИЛОЖЕНИЕ ЗАПУЩЕНО (index.tsx)      │
└──────────────┬──────────────────────────┘
               ↓
       ┌───────────────────┐
       │ Проверить состояние│
       │ аутентификации    │
       │ Токен существует? │
       └───┬───────────────┘
           │
      ДА   │  НЕТ
           ↓  ↓
    ┌──────┐ ┌──────────────────┐
    │      │ │  (auth) Группа   │
    │(main)│ │  ├─ login        │
    │ Группа│ │  ├─ register    │
    │ ├─tab│ │  └─ forgot-pwd   │
    │ ├─msg│ └──────────────────┘
    │ └─doc│
    └──────┘
       ↓
    Навигация
```

Эта структура маршрутизации обеспечивает чистую, поддерживаемую основу для навигации вашего приложения, сохраняя при этом семантичные и удобные для пользователя URL.
